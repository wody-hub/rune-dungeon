Original prompt: 자 여기는 게임 개발 공간이고 plan 디렉토리에는 우리가 개발할 게임에 대한 기획서를 작성중이야. 전체 읽고 학습해줘

Status: active
Last updated: 2026-07-30

# Progress

## 문서 기준
- `plan/`은 현재 확정 설계의 기준 문서다.
- `progress.md`는 다음 세션을 위한 현재 상태 요약이다.
- `scratchpad/policies/`는 상위 정책 결정과 과거 검토 기록이다.
- `scratchpad/design-drafts/`는 상세 설계 초안이며, `plan/`에 반영되기 전까지는 확정 기준이 아니다.

## 외부 참고 소스
- OpenMMO 참고 저장소 로컬 경로: `/Users/j.jaeyo/Project/ETC/OpenMMO`
- 원격 주소: `https://github.com/wody-hub/OpenMMO.git`
- 이 저장소는 Rune Dungeon의 3D 온라인 월드 구현을 위한 구조 참고용이다. 특히 `client/`의 Svelte + Three.js/Threlte 구조, `server/`의 Rust + WebSocket 서버 권위 구조, `shared/`의 공통 게임 규칙 구조를 참고한다.
- OpenMMO는 PolyForm Noncommercial 라이선스이므로 코드·에셋을 직접 복사하거나 Rune Dungeon에 포함하지 않는다. 구조와 구현 원칙만 참고해 독자 구현한다.

## 현재 제품 방향
- 제품 방향은 `하이브리드 액션 수집 RPG`다.
- 메인 플레이 축은 `자동/방치 성장`이다.
- 보조 축은 `수동 조작 던전`이다.
- 수동 던전은 보조 모드이지만 메인급 완성도와 보상을 가진다.
- 첫 구현 범위는 전체 제품 MVP가 아니라 `아르카디아 수직 슬라이스`다.
- `아르카디아 수직 슬라이스`는 `공유 마을 허브`인 아르카디아에서 출발해 새벽 들판, 축약 던전, 보스방의 `협동 인스턴스`를 순서대로 공략하며 수동 전투, 드랍, 제작, 장착, 변신, 보스 격파 루프를 온라인으로 검증한다.
- 마을·구역 `포털`과 입장 트리거가 인스턴스를 연결한다. 인스턴스는 미리 구성된 파티를 강제하지 않는 공개 지속형 방이며, 최대 4명이 자유롭게 입장·퇴장하고 진행 중에도 빈자리에 합류할 수 있다.

## 현재 핵심 용어
- `음`: 플레이어가 수집하는 흩어진 자음·모음 자원의 통칭이다. 자음, 모음, 초성, 중성, 종성 같은 기술 분류는 조합 규칙과 런타임 데이터 내부에서만 사용한다.
- `자형`: `음`을 100% 확정 조합해 만든 글자 형태다.
- `결`: 인벤토리 1칸을 차지하는 완성형 결과물의 총칭이다.
- `결: 글자`: 의미가 허용된 자형에 각인을 진행해 만든 단일 글자 결이다.
- `진언결`: 단어형 패시브 결이다.
- `언령결`: 문장형 확률 발동 결이다.
- `돌`: 각인 필수 촉매의 총칭이다.
- `먹`: 실패 완화용 보조 재료의 총칭이다.
- 최신 보호 재료명은 `먹: 지킴`, `먹: 남김`, `먹: 돋움`이다.

## 현재 티어 정책
- 승급 레벨 구간은 `Lv 10, 40, 70, 100, 130, 160, 190, 200`으로 고정한다.
- `씨앗`: Lv 1~9
- `움결`: Lv 10~39
- `무늬`: Lv 40~69
- `물결`: Lv 70~99
- `울림`: Lv 100~129
- `숨결`: Lv 130~159
- `빛살`: Lv 160~189
- `여울`: Lv 190~199
- `온결`: Lv 200
- `씨앗` 티어 언령결은 미도입이다.
- `움결`, `무늬`는 속성 라인마다 언령결 3개씩 운영한다.
- `움결`은 속성별 3개 중 2개를 스토리 지급, 1개를 제작 획득으로 둔다.
- `무늬`는 속성별 3개 중 1개를 스토리 지급, 2개를 제작 획득으로 둔다.
- `물결` 이후는 티어당 속성별 1개를 기본 구조로 둔다.

## 현재 전투 정책
- 전투에 별도 `마나` 자원은 없다.
- 언령결은 퀵슬롯 액티브가 아니라 기본 공격 적중 시 확률 발동하는 장착형 효과다.
- 언령결은 내부 쿨다운 없이 타격마다 독립 확률 판정을 수행한다.
- 같은 타격에서 여러 언령결이 동시에 발동할 수 있다.
- 모든 언령결은 `단일 대상 1체`만 공격한다.
- 초기 4티어(`움결`, `무늬`, `물결`, `울림`) 언령결 기본 발동률은 `3%`다.
- 고위 티어 언령결 기본 발동률은 `1%`다.
- 최종 언령 발동 확률 상한은 `20%`다.
- 언령 발동 확률은 언령결 기본값, 무기군 `procRateBonus`, 제한적 진언 옵션, 후반 `천지인` 보정으로 관리한다.
- MVP 기준 `INT`는 언령 발동 확률을 직접 올리지 않는다.
- MVP 기준 `INT`는 언령 피해, 속성 효과, 상태이상 계수 같은 효과 스케일링 후보로 둔다.

## 현재 월드/성장 정책
- 첫 수직 슬라이스에서 공유 공간은 `아르카디아`의 공유 마을 허브뿐이다. `새벽 들판`, `흑심 채굴장`, `보스방`은 입장 시 생성되는 협동 인스턴스다.
- 동선은 `아르카디아 마을 -> 새벽 들판 -> 흑심 채굴장 -> 보스방 -> 아르카디아 마을`이며, 각 구역은 포털 또는 입장 트리거로 이동한다.
- 이후 지역도 각 지역 허브와 필드/던전 인스턴스를 포털 네트워크로 연결한다.
- 메인 월드는 `아르카디아`, `드라실`, `카르멘`, `엘리시온`의 4개 연결 구역이다.
- 4개 메인 구역은 `Lv 100`까지의 성장 구간을 담당한다.
- `근원의 회랑`은 `Lv 100` 이후 진입하는 관문형 특수 구역이다.
- `근원의 나라`는 `천지인` 중심의 언령 각성 루프로 쓰는 후반/확장 영역이다.
- `근원의 회랑` 이후의 조선 배경 지역은 향후 업데이트 범위로 분리한다.

## 현재 기술 기준
- 첫 구현은 고정 아이소메트릭 3D 온라인 수직 슬라이스다.
- 클라이언트 스택은 `Svelte + TypeScript + Vite + Three.js/Threlte`다.
- 서버는 `Rust + WebSocket`이며, 플레이어 ID, 위치, 전투, 몬스터, 드랍, 인벤토리, 제작, 변신의 최종 상태를 권위적으로 결정한다.
- 접속은 게스트 닉네임으로 시작하며, 서버가 플레이어 ID와 세션 정체성을 발급한다.
- 한글 조합은 `hangul-js`를 기준으로 둔다. 런타임 JSON은 서버가 검증하는 콘텐츠 원본이고 클라이언트 사본은 표시용이다.
- 서버가 영속 상태를 소유한다. `localStorage`는 그래픽·입력·UI 같은 비권위 클라이언트 환경설정만 저장하며 게임 진행에는 사용하지 않는다.

## 아르카디아 수직 슬라이스 기준
- 마을: `아르카디아` 1개
- 필드: `새벽 들판` 1개
- 던전: `흑심 채굴장` 축약형 1개
- 보스: `몽당연필 기사단장` 1종
- 일반 몬스터: `먹물 슬라임`
- 정예 몬스터: `오타 요정`
- 무기: `대검계열` 1종
- 속성/인: `인: 움결 화` 1종
- 진언결: 소수 `2~3개`
- 언령결: `움결` 화 속성 `2개`
- 소모품: `부적: 숨길`, `부적: 결불`, `부적: 질숨`, `부적: 날숨`, `부적: 굳결`, `부적: 귀로`
- UI: `HUD`, `인벤토리`, `장착`, `제작`, `변신`, `보스 UI`
- 기술 샘플 데이터의 플레이어는 실제 Lv 1 시작 상태가 아니라 `Lv 12 움결 화` 검증 로드아웃이다.

## 2026-07-30 최신 설계 결정 — 다음 세션 우선 기준

- 상세 결정 원본: `docs/game-design-gap-checklist.md` (현재 아직 커밋되지 않은 신규 문서). 이후 세션은 이 문서를 먼저 읽고 이어서 갱신한다.
- 이 절의 최신 결정은 기존 `plan/`과 3D 수직 슬라이스 명세의 상충 문구보다 우선한다. 대화가 끝난 뒤 관련 `plan/` 문서는 별도 정합성 반영이 필요하다.

### 조작과 전투

- 이동은 방향키 또는 클릭 이동을 사용자가 선택한다. `WASD`는 향후 단축키용으로 남긴다.
- 적 클릭 또는 `Tab`으로 타깃을 선택한다. 타깃 더블클릭 또는 `Tab` 빠른 두 번 입력으로 자동공격을 시작한다.
- 캐스팅과 시전 속도 개념은 사용하지 않으며 속도 능력치는 공격 속도만 둔다.
- 플레이어 액티브 스킬 개념은 전면 폐기한다. 능동 행동은 이동, 타깃 선택, 자동공격 토글, 변신 on/off, 부적 사용뿐이다. 자동사냥은 스킬이 아니라 자동공격 토글과 향후 자동/방치 시스템으로 처리한다.
- 회피는 조작이 아닌 2단계 확률 판정이다: 회피 발동 확률, 그리고 발동 시 원래 피해의 `0~50%`만 적용될 확률. 정확한 수치·산식은 밸런스 단계에서 정한다.
- 피격 경직은 기본값에 공격 효과와 피격자의 아이템 옵션을 반영해 계산한다. 경직 무시 효과가 있거나 최종 경직값이 `0`이면 경직되지 않는다.
- 피격 경직은 아직 시작하지 않은 다음 공격만 취소하며, 이미 서버에서 판정된 피해는 취소하거나 되돌리지 않는다. 이 규칙은 OpenMMO의 몬스터 `Hit` 상태 동작을 구조 참고로 분석해 확정했다.
- 몬스터별로 발견 범위와 복귀 조건을 기본값으로 둔다. 공격 사거리는 무기별 기본값, 공격 주기는 공격 속도 기반으로 둔다. 구체적 수치·산식은 미정이다.
- 플레이어 전투 불능: 일반 필드에서는 경험치 `2%` 감소 후 마을 이동, 레벨 하락 없음. 협동 인스턴스에서는 캐릭터당 `2회`까지 동료 부활 가능, 부활 대기 `1분` 뒤 기절 상태가 된다. 기절 상태는 사망이라 부르지 않으며, 기존 전리품은 보존하지만 클리어 보상은 받지 못한다.
- 부적은 `회복`, `데미지`, `공격 속도`, `이동 속도`, `방어력` 5종과 `귀환`으로 정리한다. 회복을 포함한 5종은 각각 `30분` 동안 지속되며, 회복은 즉시 회복이 아니라 시간 경과 지속 회복이다. 이름은 `부적: 이름` 형식으로 `부적: 숨길`, `부적: 결불`, `부적: 질숨`, `부적: 날숨`, `부적: 굳결`, `부적: 귀로`를 쓴다. 증가 수치는 미정이다.

### 협동, 인스턴스, 보상

- 파티는 최대 `4인`, 디아블로 2식 직접 초대·수락의 선택적 협업 도구다. 파티장은 파티가 함께 새 인스턴스를 열 때 생성·입장 확인만 담당하며, 초대·강퇴·전리품 권한은 없다.
- 자동 매칭은 사용하지 않는다. 마을 모집 목록으로 직접 신청·초대한다.
- 인스턴스 입장에 파티는 필수가 아니다. 모든 인스턴스는 목록에 공개되고, 비밀번호 미설정 방은 빈자리가 있으면 누구나 합류한다. 비밀번호 방도 목록에 잠금 상태로 표시되며, 코드를 아는 사람만 합류한다.
- 새벽 들판·흑심 채굴장·보스방은 최대 4인이 자유롭게 드나드는 지속형 방이다. 진행이 어려우면 길드원 등에게 요청해 바로 합류를 부탁할 수 있다.
- 접속이 끊긴 캐릭터의 자리·인스턴스 상태는 `3분` 유지한다. 캐릭터는 제자리에 남아 공격받을 수 있고, 시간 내 재접속하면 같은 위치·전리품 상태로 복귀한다. 시간 초과면 이탈 처리한다.
- 마지막 플레이어가 나간 빈 인스턴스는 `5분` 유지한다. 이 안에 누군가 들어오면 기존 상태를 이어가고, 아무도 들어오지 않으면 종료한다.
- 클리어 뒤 방을 즉시 닫지 않는다. 남은 플레이어는 정리·탐험을 계속할 수 있고, 목록에는 `클리어` 상태로 표시한다.
- 전리품은 디아블로 4식 개인 독립 판정이며 즉시 해당 캐릭터에 저장한다. 클리어 보상은 목표 처치 순간 방 안에 있고 전투 불능이 아닌 플레이어만 받는다. 인벤토리가 가득 차면 알림을 보이고 빈칸을 만들기 전까지 새 전리품을 얻지 못한다.

### 제작, 보스, 스토리

- 첫 제작은 `음`, `돌`, `골드` 세 재료로 `자형·결: 글자`를 100% 제작하는 단순 순환이다. 음은 몬스터·상자, 돌은 흑심 채굴장, 골드는 전투·판매에서 얻는다. 제작 결과는 공격력·체력·경직 저항 중 하나를 확실히 올려 첫 보스전에서 체감되게 한다. 첫 수직 슬라이스에는 제작 실패·강화·희귀 조합을 넣지 않는다.
- `결: 글자` 각인 촉매는 `돌: 새김`을 재사용하며, 역할을 `결: 글자·진언결 각인 촉매`로 확장한다. 첫 레시피 계약은 `자형: 화`(음 2개, 100%), `결: 화`(자형: 화 + 돌: 새김 + 골드 20, 100%)로 고정한다.
- 모든 속성 보스는 자기 속성 언령결 제작용 `돌: 말문`을 저확률 직드랍과 `돌: 말문 조각` 누적 보장으로 공급한다.
- 제작 언령결의 완성 시점은 티어 레벨 구간에 묶지 않는다. 해당 속성 보스를 처음 만나는 지역에서 제작이 열리며, 속성별 완성 시점 차이는 의도된 설계다. 그 전 구간은 스토리 지급 언령결만으로 전투가 성립해야 한다.
- 첫 제작 재료 동선: `음 · ㅎ`, `음 · ㅘ`와 `돌: 새김`은 흑심 채굴장(정예 `오타 요정`, 상자)에서 모이고, 새벽 들판은 골드와 기본 음 위주다. 상자의 위치·보상은 첫 20분 스토리 팩에서 확정한다.
- 몽당연필 기사단장 보스전은 고정 아이소메트릭 시점의 디아블로 2식 패턴으로 설계한다. 카메라 회전·시점 전환 없이 준비 동작, 투사체, 범위 공격, 소환 등 화면에서 읽을 수 있는 패턴만 쓴다. 고유 패턴·그로기·공략 시간은 아직 미정이다.
- 세계관과 메인 스토리는 이미 `plan/14_Story_and_World_Lore.md`에 있다. 현재 필요한 것은 세계관 추가가 아니라 플레이어가 실제로 겪는 `첫 20분 스토리 팩`이다.
- 퀘스트는 디아블로 2식으로, 반드시 존재하되 다른 사냥·제작·공개 인스턴스 활동을 잠그지 않는다. 퀘스트 행동은 먼저 해도 기록하고, 파티 협력과 개인 퀘스트 상태·보상은 서버가 분리 판정한다.
- 첫 구현은 자체 데이터 기반 `Quest`(시작 조건/목표 이벤트/완료 조건/보상/다음 퀘스트)와 `Dialogue`(NPC/표시 조건/짧은 대사/선택지/서버 행동 키)로 만든다. 서버가 진행·보상·해금을 권위적으로 저장하고 클라이언트는 표시만 한다.
- Ink와 Yarn Spinner를 조사했다. 첫 수직 슬라이스에는 도입하지 않는다. 선택지·개인 서사가 커지면 Yarn Spinner을 먼저 검토하고, 긴 분기형 서사가 필요해질 때 Ink를 검토한다.

### 다음 논의 순서

1. `첫 20분 스토리 팩`: 나랏의 첫 대사, 퀘스트 `4~5개`, 각 단계의 목표·해금·개인 보상·다음 행동을 작성한다. 함께 확정할 것: 흑심 채굴장 상자의 위치·보상, 씨앗(Lv 1~9)에서 움결(Lv 10) 도달 페이스와 `인: 움결 화`·언령결 체험 시점. 가르칠 루프는 `이동·타깃·자동공격 -> 새벽 들판 사냥 -> 흑심 채굴장(ㅎ·ㅘ, 돌: 새김) -> 첫 제작 -> 장착 -> 변신 -> 보스 격파`다.
2. 몽당연필 기사단장: 고유 패턴, 약점 노출·그로기 규칙, 목표 공략 시간을 확정한다.
3. UI 첫 화면 흐름과 3D 임시 에셋 기준을 정한다. 액티브 스킬 폐기로 스킬바·퀵슬롯 설계는 범위에서 제외됐고, HUD는 체력·타깃·변신 상태·부적 슬롯 중심이다.
4. ~~새 결정과 상충한 문서 정합화~~ 완료: 2026-07-30 전수 교차 검토로 `plan/` 전체·런타임 데이터를 최신 결정에 정합화했다(아래 `2026-07-30 정합화 반영` 참조).

## 2026-07-30 정합화 반영
- 물약 체계를 `부적` 계열로 전환: `부적: 숨길/결불/질숨/날숨/굳결/귀로` 6종, 회복 포함 5종은 `30분` 지속, 수치 미정(`needsNumericTuning`).
- 첫 제작 계약을 `음+돌+골드 / 100%`로 통일: `결: 화` = 자형: 화 + 돌: 새김 + 골드 20. 검증 스크립트가 새 계약을 강제한다.
- 회피 2단계 판정을 plan/02·03·04·16에 반영. 산식·수치는 밸런스 단계.
- 인스턴스 공개 지속형 방 규칙을 plan/01·04·14·17에 반영. 구식 "같은 파티 또는 초대된 게스트만" 서술 제거.
- plan/13 도감을 구역 기반으로 재편(새벽 들판/흑심 채굴장/드라실 먹빛 서고), 보스 `돌: 말문` 공급 규칙 추가.
- plan/02 자모 매핑을 설계 지침으로 명문화(실능력치 정본은 plan/09·gyeol-balance), `결: 강화` 예시를 런타임 데이터와 통일.
- 경직/스턴 용어 구분(plan/03), px=논리 거리 단위 환산 기준(plan/04), INT 0.5%는 1차 후보값 표기.

## 최근 정합성 정리 완료
- `plan/17_MVP_Development_Roadmap.md`를 `아르카디아 수직 슬라이스 개발 단계표`로 재작성했다.
- `plan/04_Technical_Architecture.md`를 구현 기준 레퍼런스로 재작성했다.
- `plan/04`의 `player.json` 샘플을 `Lv 12 움결 화` 검증 로드아웃으로 명시했다.
- `plan/04`의 MVP 언령결 샘플을 `움결` 2개로 맞췄다.
- `plan/04`의 보스 상태 예외에 `groggyDefenseOverride`를 추가했다.
- `plan/03`, `plan/16`, `plan/04`에서 `INT`의 직접 언령 발동 확률 보정을 제거했다.
- `plan/01`에 첫 구현 범위가 전체 제품 MVP가 아니라 수직 슬라이스라는 기준을 추가했다.
- 이 파일을 현재 기준 핸드오프로 재작성했다.
- `plan/05_Educational_System.md`, `plan/09_Data_Sheet_Runes.md`, `plan/14_Story_and_World_Lore.md`의 문서 파손을 정리했다.
- `plan/09_Data_Sheet_Runes.md`를 사람이 읽는 기준표로 재정리하고, 구현 후보 JSON 위치를 명시했다.
- `data/design/gyeol-balance.json`에 자형, 결: 글자, 진언결, 언령결, 발동률, 무기군 발동 기준, 보호/촉매 재료를 정규화했다.
- `data/design/README.md`에 `plan/` 원본과 `data/design/*.json`의 역할 분리를 기록했다.
- `client/src/game/types/data.ts`에 첫 수직 슬라이스 런타임 데이터 타입 초안을 추가했다.
- `client/src/game/data/`에 플레이어, 무기, 결, 몬스터, 소모품, 재료, 인, 제작, 월드 콘텐츠 전환용 fixture를 분리했다.
- 몬스터별 명시적 드랍 계약과 `1~2`/`2~4`/`3~5` 그룹 음 보상, 첫 제작 레시피가 이미 존재한다.
- `scripts/validate-runtime-data.mjs`로 런타임 JSON의 필수 참조와 언령 발동 정책을 검증한다.
- 첫 구현의 3D 온라인 기준과 구버전 2D/로컬 기준을 문서 가드로 함께 검증한다.

## 폐기 또는 구버전 결정
- 언령결 기본 발동률 `초기 5%`, `후반 2%` 정책은 폐기한다.
- 언령결 개별 내부 쿨다운 정책은 폐기한다.
- 언령결 수동 시전/액티브 스킬 전제는 폐기한다.
- `지킴결`, `남은결`, `돋움결` 명칭은 구버전이다.
- 최신 명칭은 `먹: 지킴`, `먹: 남김`, `먹: 돋움`이다.
- `INT 1 = 언령 발동 확률 +0.2%` 정책은 MVP 기준 폐기한다.

## 다음 구현 계획
- 다음 구현 계획은 고정 아이소메트릭 3D `Svelte + TypeScript + Vite + Three.js/Threlte` 클라이언트와 `Rust + WebSocket` 서버 기반을 함께 만드는 것이다.
- `client/`에는 고정 아이소메트릭 카메라, 게스트 접속 UI, 서버 상태를 표시하는 최소 렌더링 골격을 만든다.
- `server/`에는 게스트 ID와 세션, 서버 권위 월드 상태, 의도 검증과 상태 브로드캐스트 골격을 만든다.
- 현재 `client/src/game/data` 전환용 fixture를 `shared/content`의 서버 정본 콘텐츠로 옮기고, 검증된 정본에서 클라이언트 표시용 사본을 생성하는 로더를 구현한다.
- 이미 명시된 골드·아이템 항목과 그룹 음 보상 계약을 서버의 사망 1회 단일 판정으로 구현하고, 중복 보상 지급을 막는다.
- 첫 구현용 진언결은 `결: 강화` 1개만 런타임 확정했다. 추가 진언결 `1~2개`는 수치 확정 후 넣는다.
- `인: 움결 화`의 3D 모델·리그·애니메이션 클립·무기 모델·오라 키 계약은 확정했으며, 실제 자산은 클라이언트 구현 때 연결한다.
- OpenMMO는 Svelte/Three 클라이언트와 Rust/WebSocket 서버의 경계·구조를 이해하기 위한 아키텍처 참고만으로 사용한다. 코드, 에셋, 프로토콜 이름, 콘텐츠를 복사하지 않는다.
